;Ｃ感覚の可否
@ISABLUP3
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:3 >= 10
	RETURN 0
ELSEIF ABL:3 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	RETURN 0
ENDIF

CALL DECIDE_ABLUP3

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF

;Ｃ感覚
@ABLUP3
DRAWLINE
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:3 >= 10
	PRINTW 既にMAXです
	RETURN 0
ELSEIF ABL:3 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	PRINTW 既にMAXです
	RETURN 0
ENDIF

CALL DECIDE_ABLUP3

PRINT [0] - 
PRINTS PALAMNAME:0
PRINT の珠×
PRINTV A
PRINT ……
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 珠不足 
	SIF I & 2
		PRINT 経験不足 
ENDIF
PRINTL 

PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 条件を満たしていません
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:3 += 1

IF RESULT == 0
	JUEL:0 -= A
ENDIF

PRINTS ABLNAME:3
PRINT のレベルが
PRINTV ABL:3
PRINTW になりました


;C感覚LvUP判定
@DECIDE_ABLUP3
;条件別にＯＫかダメかを記録する
I = 0
;能力アップ可能なら1にする
Z = 0

IF ABL:3 == 0
	A = 1
ELSEIF ABL:3 == 1
	A = 20
ELSEIF ABL:3 == 2
	A = 400
ELSEIF ABL:3 == 3
	A = 5000
ELSEIF ABL:3 == 4
	A = 15000
ELSEIF ABL:3 == 5
	A = 30000
ELSEIF ABL:3 == 6
	A = 40000
ELSEIF ABL:3 == 7
	A = 50000
ELSEIF ABL:3 == 8
	A = 60000
ELSEIF ABL:3 == 9
	A = 70000
ENDIF

;淫乱
SIF TALENT:淫乱
	TIMES A , 0.80
;娼婦
SIF TALENT:娼婦
	TIMES A , 0.50

;0になった場合は1にする
SIF A == 0
	A = 1

;快Ｃの珠で上げる
SIF JUEL:0 < A
	I |= 1

IF I == 0
	Z = 1
ENDIF
RETURN 1

