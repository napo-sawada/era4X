;Ｂ感覚の可否
@ISABLUP6
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:6 >= 10
	RETURN 0
ELSEIF ABL:6 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	RETURN 0
ENDIF

CALL DECIDE_ABLUP6

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;Ｂ感覚
@ABLUP6
DRAWLINE
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:6 >= 10
	PRINTW 既にMAXです
	RETURN 0
ELSEIF ABL:6 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	PRINTW 既にMAXです
	RETURN 0
ENDIF

CALL DECIDE_ABLUP6

PRINT [0] - 
PRINTS PALAMNAME:3
PRINT の珠×
PRINTV A
PRINT ……
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 珠不足 
	SIF I & 2
		PRINT 経験不足 
ENDIF
PRINTL 

PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 条件を満たしていません
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:6 += 1

IF RESULT == 0
	JUEL:3 -= A
ENDIF

PRINTS ABLNAME:6
PRINT のレベルが
PRINTV ABL:6
PRINTW になりました


;Ｂ感覚LVUP判定
@DECIDE_ABLUP6
;条件別にＯＫかダメかを記録する
I = 0
;能力アップ可能なら1にする
Z = 0

IF ABL:6 == 0
	A = 1
ELSEIF ABL:6 == 1
	A = 20
ELSEIF ABL:6 == 2
	A = 400
ELSEIF ABL:6 == 3
	A = 5000
ELSEIF ABL:6 == 4
	A = 15000
ELSEIF ABL:6 == 5
	A = 30000
ELSEIF ABL:6 == 6
	A = 40000
ELSEIF ABL:6 == 7
	A = 50000
ELSEIF ABL:6 == 8
	A = 60000
ELSEIF ABL:6 == 9
	A = 70000
ENDIF

;淫乱
SIF TALENT:淫乱
	TIMES A , 0.80
;娼婦
SIF TALENT:娼婦
	TIMES A , 0.50

;貧乳
IF TALENT:貧乳
	TIMES A , 0.80
ENDIF

;絶壁
IF TALENT:絶壁
	TIMES A , 0.65
ENDIF

;巨乳
IF TALENT:巨乳
	TIMES A , 1.10
ENDIF

;爆乳
IF TALENT:爆乳
	TIMES A , 1.15
ENDIF

;0になった場合は1にする
SIF A == 0
	A = 1

;快Ｂの珠で上げる
SIF JUEL:3 < A
	I |= 1

IF I == 0
	Z = 1
ENDIF

