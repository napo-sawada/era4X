@SHOW_INFO(ID)
#DIM ID
PRINTL
PRINTFORML 名前   %NAME:ID%
PRINTFORML 呼び名 %CALLNAME:ID%
PRINTFORML 体力   {BASE:ID:0}/{MAXBASE:ID:0}
PRINTFORML 気力   {BASE:ID:1}/{MAXBASE:ID:1}

PRINTL 
PRINTL □ 経験
CALL SHOW_INFO_EXP(ID)
PRINTL 
PRINTL □ 能力
CALL SHOW_INFO_ABL(ID)
PRINTL
IF ID != MASTER
	PRINTL □ 刻印
	CALL SHOW_INFO_MARK(ID)
	PRINTL 
ENDIF
PRINTL □ 素質
CALL SHOW_INFO_TALENT(ID)
PRINTL
PRINTL □ 身体的特徴
CALL SHO_INFO_BODY(ID)
PRINTL
;IF ID != MASTER
;	PRINTL □ 服装
;	CALL SHOW_INFO_CLOTHES(ID)
;	PRINTL 
; ENDIF

@SHOW_STATUS
;DOTRAIN終了時に折る
;またCALLTRAINがDOTRAINで最後までいけなかったときのために折る
TFLAG:DOTRAIN = 0
TFLAG:CALLTRAIN = 0
DRAWLINE
PRINTV DAY
PRINT 日目
IF TIME == 0
	PRINTFORML (昼)  所持金：${MONEY}
ELSE
	PRINTFORML (夜)  所持金：${MONEY}
ENDIF
PRINTFORM %NAME:TARGET% 調教中 
IF ASSIPLAY
	PRINTFORM   調教者:
	PRINTS NAME:ASSI
	PRINTL (助手)
ELSE
	PRINTFORM   調教者:
	PRINTSL NAME:MASTER
ENDIF
IF TALENT:PLAYER:テレパシー
	PRINTFORM (依存度:{CFLAG:2} ストレス値{CFLAG:10})
ENDIF
PRINT   

CALL SHOW_EQUIP_2
CALL LIFE_BAR

PRINT 気力
A = BASE:1
SIF BASE:1 < 0
	A = 0
BAR A,MAXBASE:1,32
PRINTFORML ({A}/{MAXBASE:1})

CALL SHOW_INFO_PALAM

B = 10000
SIF TALENT:MASTER:絶倫
	B /= 2
MAXBASE:MASTER:2 = B

SIF MAXBASE:2 == 0
	MAXBASE:2 = 10000
IF ASSI >= 0
	SIF MAXBASE:ASSI:2 == 0
		MAXBASE:ASSI:2 = 10000
ENDIF

IF TALENT:MASTER:半人半妖 || TALENT:MASTER:ふたなり || TALENT:MASTER:オトコ
	PRINT 射精
	BAR BASE:MASTER:2,MAXBASE:MASTER:2,32
	PRINTFORML ({BASE:MASTER:2}/{MAXBASE:MASTER:2})
ENDIF

IF ASSIPLAY
	IF TALENT:ASSI:半人半妖 || TALENT:ASSI:ふたなり || TALENT:ASSI:オトコ
		PRINT 射精（助手）
		BAR BASE:ASSI:2,MAXBASE:ASSI:2,32
		PRINTFORML ({BASE:ASSI:2}/{MAXBASE:ASSI:2})
	ENDIF
ENDIF

IF TALENT:TARGET:半人半妖 || TALENT:TARGET:ふたなり || TALENT:TARGET:オトコ
	PRINT 射精（
	PRINTS NAME:TARGET
	PRINT ）
	BAR BASE:2,MAXBASE:2,32
	PRINTFORML ({BASE:2}/{MAXBASE:2})
ENDIF

IF TALENT:TARGET:母乳体質
	B = 10000
	SIF TALENT:TARGET:淫乳
		B /= 2
	MAXBASE:3 = B
	PRINT 母乳（
	PRINTS NAME:TARGET
	PRINT ）
	BAR BASE:3,MAXBASE:3,32
	PRINTFORML ({BASE:3}/{MAXBASE:3})
ENDIF

IF TEQUIP:25
	SIF MAXBASE:MASTER:4 == 0
		MAXBASE:MASTER:4 = 10000
	PRINT 射精（触手）
	BAR BASE:MASTER:4,MAXBASE:MASTER:4,32
	PRINTFORML ({BASE:MASTER:4}/{MAXBASE:MASTER:4})
ENDIF
DRAWLINE

CALL SHOW_EQUIP_1


@LIFE_BAR
PRINT 体力
A = BASE:0
SIF BASE:0 < 0
	A = 0
BAR A,MAXBASE:0,32
PRINTFORML ({A}/{MAXBASE:0})

@STAIN_INFO

;主人の汚れ
PRINTFORML 【%CALLNAME:MASTER%の汚れ】
REPEAT 7
	SIF COUNT == 2 && TALENT:MASTER:半人半妖 == 0 && TALENT:MASTER:ふたなり == 0 && TALENT:MASTER:オトコ == 0
		CONTINUE
	SIF COUNT == 3 && TALENT:MASTER:オトコ
		CONTINUE
	SIF COUNT == 5 && TALENT:MASTER:オトコ
		CONTINUE
	SIF COUNT == 6 && TALENT:MASTER:オトコ
		CONTINUE
	IF COUNT == 0
		PRINT 　口：
	ELSEIF COUNT == 1
		PRINT 　手：
	ELSEIF COUNT == 2
		PRINT 　Ｐ：
	ELSEIF COUNT == 3
		PRINT 　Ｖ：
	ELSEIF COUNT == 4
		PRINT 　Ａ：
	ELSEIF COUNT == 5
		PRINT 　Ｂ：
	ELSEIF COUNT == 6
		PRINT 　膣：
	ENDIF
	SIF STAIN:MASTER:COUNT & 1
		PRINT <愛液>
	SIF STAIN:MASTER:COUNT & 2
		PRINT <ペニス>
	SIF STAIN:MASTER:COUNT & 4
		PRINT <精液>
	SIF STAIN:MASTER:COUNT & 8
		PRINT <アナル>
	SIF STAIN:MASTER:COUNT & 16
		PRINT <母乳>
	SIF STAIN:MASTER:COUNT & 32
		PRINT <粘液>
	SIF STAIN:MASTER:COUNT & 64
		PRINT <破瓜の血>
	PRINTL 
REND
PRINTL 

;奴隷の汚れ
PRINTFORML 【%CALLNAME:TARGET%の汚れ】
REPEAT 7
	SIF COUNT == 2 && TALENT:半人半妖 == 0 && TALENT:ふたなり == 0 && TALENT:オトコ == 0
		CONTINUE
	SIF COUNT == 3 && TALENT:オトコ
		CONTINUE
	SIF COUNT == 5 && TALENT:オトコ
		CONTINUE
	SIF COUNT == 6 && TALENT:オトコ
		CONTINUE
	IF COUNT == 0
		PRINT 　口：
	ELSEIF COUNT == 1
		PRINT 　手：
	ELSEIF COUNT == 2
		PRINT 　Ｐ：
	ELSEIF COUNT == 3
		PRINT 　Ｖ：
	ELSEIF COUNT == 4
		PRINT 　Ａ：
	ELSEIF COUNT == 5
		PRINT 　Ｂ：
	ELSEIF COUNT == 6
		PRINT 　膣：
	ENDIF
	SIF STAIN:COUNT & 1
		PRINT <愛液>
	SIF STAIN:COUNT & 2
		PRINT <ペニス>
	SIF STAIN:COUNT & 4
		PRINT <精液>
	SIF STAIN:COUNT & 8
		PRINT <アナル>
	SIF STAIN:COUNT & 16
		PRINT <母乳>
	SIF STAIN:COUNT & 32
		PRINT <粘液>
	SIF STAIN:COUNT & 64
		PRINT <破瓜の血>
	PRINTL 
REND
PRINTL 

;助手の汚れ
SIF ASSI >= 0
	PRINTFORML 【%CALLNAME:ASSI%の汚れ】
REPEAT 7
	SIF ASSI < 0
		BREAK
	SIF COUNT == 2 && TALENT:ASSI:半人半妖 == 0 && TALENT:ASSI:ふたなり == 0 && TALENT:ASSI:オトコ == 0
		CONTINUE
	SIF COUNT == 3 && TALENT:ASSI:オトコ
		CONTINUE
	SIF COUNT == 5 && TALENT:ASSI:オトコ
		CONTINUE
	SIF COUNT == 6 && TALENT:ASSI:オトコ
		CONTINUE
	IF COUNT == 0
		PRINT 　口：
	ELSEIF COUNT == 1
		PRINT 　手：
	ELSEIF COUNT == 2
		PRINT 　Ｐ：
	ELSEIF COUNT == 3
		PRINT 　Ｖ：
	ELSEIF COUNT == 4
		PRINT 　Ａ：
	ELSEIF COUNT == 5
		PRINT 　Ｂ：
	ELSEIF COUNT == 6
		PRINT 　膣：
	ENDIF
	SIF STAIN:ASSI:COUNT & 1
		PRINT <愛液>
	SIF STAIN:ASSI:COUNT & 2
		PRINT <ペニス>
	SIF STAIN:ASSI:COUNT & 4
		PRINT <精液>
	SIF STAIN:ASSI:COUNT & 8
		PRINT <アナル>
	SIF STAIN:ASSI:COUNT & 16
		PRINT <母乳>
	SIF STAIN:ASSI:COUNT & 32
		PRINT <粘液>
	SIF STAIN:ASSI:COUNT & 64
		PRINT <破瓜の血>
	PRINTL 
REND

WAIT

@SAVEINFO

IF TIME == 0
	PUTFORM {DAY}日目昼 
ELSE
	PUTFORM {DAY}日目夜 
ENDIF

PUTFORM 難易度:
IF FLAG:4 == 1
	PUTFORM E 
ELSEIF FLAG:4 == 2
	PUTFORM N 
ELSEIF FLAG:4 == 3
	PUTFORM H 
ENDIF

SIF FLAG:5 == 9
	PUTFORM ☆ 

IF TARGET >= 0
	PUTFORM %CALLNAME:TARGET%調教中 
	SIF TALENT:恋慕
		PUTFORM 恋慕 
	SIF TALENT:淫乱
		PUTFORM 淫乱 
	SIF TALENT:服従
		PUTFORM 服従 
	SIF TALENT:親愛
		PUTFORM 親愛 
	SIF TALENT:娼婦
		PUTFORM 娼婦 
	SIF TALENT:隷属
		PUTFORM 隷属 
	SIF TALENT:崩壊
		PUTFORM 崩壊 
	PUTFORM 従{ABL:0}欲{ABL:1}技{ABL:2}
ENDIF


@SHOW_EQUIP_2
;状況の表示
;調教対象には使わない、特殊な着脱式の道具を追加した場合はここに追加をしてください
;（場所変更、マスターや助手が使用する道具等）
IF TEQUIP:10
	IF NO:TARGET == 26
		PRINTFORM  [ハクタク化]
	ELSEIF ASSIPLAY
		PRINTFORM  [ハクタク化](助手)
	ENDIF
ENDIF
IF TEQUIP:28
		V = 10 - TFLAG:70 + 1
		PRINTFORM  [撮影中(残{V}回　撮影者：
		IF FLAG:22 == 2
			PRINTFORM %CALLNAME:ASSI%)]
		ELSE
			PRINTFORM %CALLNAME:MASTER%)]
		ENDIF
ENDIF
SIF TEQUIP:25
	PRINT  [触手召喚中] 
SIF TEQUIP:29
	PRINT  [野外プレイ中]
SIF TEQUIP:30 == 1
	PRINT  [羞恥プレイ中]
SIF TEQUIP:31
	PRINT  [お風呂場プレイ中]
SIF TEQUIP:33
	PRINT  [新妻プレイ中]
PRINTL  

@SHOW_EQUIP_1
;使用中の道具を表示する
;一度使用したら解除するまで止まらない道具や調教を
;追加した場合はここに追加をしてください

IF TEQUIP:11 || TEQUIP:13 || TEQUIP:14 || TEQUIP:15 || TEQUIP:16 || TEQUIP:17 || TEQUIP:18 || TEQUIP:19 || TEQUIP:20 || TEQUIP:21 || TEQUIP:22 || TEQUIP:23 || TEQUIP:24 || TEQUIP:26 || TEQUIP:27 || TEQUIP:32 || TEQUIP:34 || TEQUIP:36
	PRINTFORM 使用中(%NAME:TARGET%) 

SIF TEQUIP:26
	PRINT [媚薬効果発揮中]
SIF TEQUIP:27
	PRINT [利尿剤効果発揮中]
IF TEQUIP:34 == 1
	PRINT [強精神薬反動中]
ELSEIF TEQUIP:34 > 1
	PRINT [強精神薬効果発揮中]
ENDIF

IF TEQUIP:11 && TEQUIP:25
	PRINT [触手クリ責め]
ELSEIF TEQUIP:11
	PRINT [クリキャップ]
ENDIF
IF TEQUIP:12 && TEQUIP:25
	PRINT [触手ペニス責め]
ELSEIF TEQUIP:12
	PRINT [オナホール]
ENDIF
IF TEQUIP:13 && TEQUIP:25
	PRINT [触手挿入]
ELSEIF TEQUIP:13
	PRINT [バイブ]
ENDIF
IF TEQUIP:14 && TEQUIP:25
	PRINT [触手アナル挿入]
ELSEIF TEQUIP:14
	PRINT [アナルバイブ]
ENDIF
SIF TEQUIP:15
	PRINT [アナルビーズ]
IF TEQUIP:16 && TEQUIP:25
	PRINT [触手乳首責め]
ELSEIF TEQUIP:16
	PRINT [ニプルキャップ]
ENDIF
IF TEQUIP:17 && TEQUIP:25
	PRINT [触手搾乳]
ELSEIF TEQUIP:17
	PRINT [搾乳器]
ENDIF
SIF TEQUIP:18 && TEQUIP:73 == 0
	PRINT [アイマスク]
SIF TEQUIP:19 && TEQUIP:72 == 0 && TEQUIP:73 == 0
	PRINT [縄で緊縛]
SIF TEQUIP:19 && TEQUIP:25
	PRINT [触手緊縛]
SIF TEQUIP:20
	PRINT [ボールギャグ]
IF TEQUIP:21 && TEQUIP:25
	PRINT [触手浣腸]
ELSEIF TEQUIP:21
	PRINT [浣腸＋アナルプラグ]
ENDIF
SIF TEQUIP:22
	PRINT [拡張バルーン]
SIF TEQUIP:23
	PRINT [アナル電極]
SIF TEQUIP:24
	PRINT [乳房電極装着中]
SIF TEQUIP:32
	PRINT [シャワー]
SIF TEQUIP:36
	PRINT [触手口辱]


;ココより↑にTEQUIPに登録したものを書き込む
PRINTL  
ENDIF

