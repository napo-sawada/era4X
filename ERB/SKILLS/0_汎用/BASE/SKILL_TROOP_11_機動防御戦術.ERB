;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_TROOP_11_EXIST
RETURN 1

@SKILL_0_TROOP_11_NAME
RESULTS = 機動防御戦術

;レベルは1-5まで
@SKILL_0_TROOP_11_LEVEL
RETURN 2
	
@SKILL_0_TROOP_11_EXPLANATION
RESULTS = 巡洋艦、戦艦の与ダメージ、命中率+10\% 回避率-20\% 駆逐艦以下の与/被ダメージ+20\%(必要知略100)


@SKILL_0_TROOP_11_EFFECT(VALUE, TYPE, CLASS, CH_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CH_ID

LOCAL = ABL:CH_ID:知略
	IF LOCAL >= 100
		IF TYPE == TYPE_命中 && (CLASS == 巡洋艦 || CLASS == 戦艦 || CLASS == 艦種_航空機)
			TIMES VALUE, 1.10
		ELSEIF TYPE == TYPE_与ダメージ && (CLASS == 巡洋艦 || CLASS == 戦艦 || CLASS == 艦種_航空機)
			TIMES VALUE, 1.10
		ELSEIF TYPE == TYPE_回避 && (CLASS == 巡洋艦 || CLASS == 戦艦 || CLASS == 空母)
			TIMES VALUE, 0.80
		ELSEIF TYPE == TYPE_与ダメージ && (CLASS == コルベット || CLASS == フリゲート || CLASS == 駆逐艦)
			TIMES VALUE, 1.20
		ELSEIF TYPE == TYPE_被ダメージ && (CLASS == コルベット || CLASS == フリゲート || CLASS == 駆逐艦)
			TIMES VALUE, 1.20
		ENDIF
	ELSE
		IF TYPE == TYPE_回避 && (CLASS == 巡洋艦 || CLASS == 戦艦 || CLASS == 空母)
			TIMES VALUE, 0.60
		ELSEIF TYPE == TYPE_命中 && (CLASS == 巡洋艦 || CLASS == 戦艦 || CLASS == 艦種_航空機)
			TIMES VALUE, 1.05
		ELSEIF TYPE == TYPE_与ダメージ && (CLASS == コルベット || CLASS == フリゲート || CLASS == 駆逐艦)
			TIMES VALUE, 1.05
		ELSEIF TYPE == TYPE_被ダメージ && (CLASS == コルベット || CLASS == フリゲート || CLASS == 駆逐艦)
			TIMES VALUE, 1.11
		ENDIF
	ENDIF
RETURN VALUE



@SKILL_0_TROOP_11_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
