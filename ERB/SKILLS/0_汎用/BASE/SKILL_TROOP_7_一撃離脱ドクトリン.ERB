;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_TROOP_7_EXIST
RETURN 1

@SKILL_0_TROOP_7_NAME
RESULTS = 一撃離脱ドクトリン

;レベルは1-5まで
@SKILL_0_TROOP_7_LEVEL
RETURN 3

@SKILL_0_TROOP_7_EXPLANATION
RESULTS = 艦隊の巡洋艦と戦艦の与ダメージ-20\%、艦隊のコルベットとフリゲートの回避率+10\% 艦隊のコルベットとフリゲートの命中率+10\％


@SKILL_0_TROOP_7_EFFECT(VALUE, TYPE, CLASS)
#DIM VALUE
#DIM TYPE
#DIM CLASS

IF TYPE == TYPE_与ダメージ && (CLASS == 巡洋艦 || CLASS == 戦艦)
	TIMES VALUE, 0.80
ELSEIF TYPE == TYPE_回避 && (CLASS == コルベット || CLASS == フリゲート)
	TIMES VALUE, 1.10
ELSEIF TYPE == TYPE_命中 && (CLASS == コルベット || CLASS == フリゲート)
	TIMES VALUE, 1.10
ENDIF

RETURN VALUE

@SKILL_0_TROOP_7_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
