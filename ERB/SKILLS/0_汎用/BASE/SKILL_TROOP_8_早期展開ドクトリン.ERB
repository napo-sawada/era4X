;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_TROOP_8_EXIST
RETURN 1

@SKILL_0_TROOP_8_NAME
RESULTS = 早期展開ドクトリン

;レベルは1-5まで
@SKILL_0_TROOP_8_LEVEL
RETURN 3

@SKILL_0_TROOP_8_EXPLANATION
RESULTS = 艦隊の与ダメージと回避+10\%、艦隊の命中-14\%(必要知略40)


@SKILL_0_TROOP_8_EFFECT(VALUE, TYPE, CLASS, CH_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CH_ID

LOCAL = ABL:CH_ID:知略
	IF LOCAL >= 40
		IF TYPE == TYPE_与ダメージ
			TIMES VALUE, 1.10
		ELSEIF TYPE == TYPE_回避
			TIMES VALUE, 1.10
		ELSEIF TYPE == TYPE_命中
			TIMES VALUE, 0.86
		ENDIF
	ELSE
		IF TYPE == TYPE_与ダメージ
			TIMES VALUE, 1.05
		ELSEIF TYPE == TYPE_回避
			TIMES VALUE, 0.95
		ELSEIF TYPE == TYPE_命中
			TIMES VALUE, 0.80
		ENDIF
	ENDIF

RETURN VALUE

@SKILL_0_TROOP_8_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
