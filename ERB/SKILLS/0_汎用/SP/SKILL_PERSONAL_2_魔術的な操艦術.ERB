;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_PERSONAL_2_EXIST
RETURN 1

@SKILL_0_PERSONAL_2_NAME
RESULTS = 魔術的な操艦術

;レベルは1-5まで
@SKILL_0_PERSONAL_2_LEVEL
RETURN 3

@SKILL_0_PERSONAL_2_EXPLANATION
RESULTS = 自艦の回避+25\%(必要操艦80)

@SKILL_0_PERSONAL_2_EFFECT(VALUE, TYPE, CLASS, CHARA_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CHARA_ID

LOCAL = ABL:CHARA_ID:操艦
	IF LOCAL >= 80
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.25
	ELSE
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.02
	ENDIF
RETURN VALUE


@SKILL_0_PERSONAL_2_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
