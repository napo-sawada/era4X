;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_PERSONAL_13_EXIST
RETURN 1

@SKILL_0_PERSONAL_13_NAME
RESULTS = 空間把握能力

;レベルは1-5まで
@SKILL_0_PERSONAL_13_LEVEL
RETURN 3

@SKILL_0_PERSONAL_13_EXPLANATION
RESULTS = 自艦の命中回避、敵回避率が『超能力』によってランダム増減。(推奨超能力30)

@SKILL_0_PERSONAL_13_EFFECT(VALUE, TYPE, CLASS, CHARA_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CHARA_ID

LOCAL = RAND(MAX(ABL:CHARA_ID:超能力, 1))
	IF LOCAL < 10
	;おっとそれは勘違いだ。上げにくさから強め。
		SIF TYPE == TYPE_命中
			TIMES VALUE, 0.90
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 0.90
		SIF TYPE == TYPE_回避
			TIMES VALUE, 0.90
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 1.2
	ELSEIF LOCAL < 20
		SIF TYPE == TYPE_命中
			TIMES VALUE, 1.10
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 1.10
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.10
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 0.9
	ELSEIF LOCAL < 50
		SIF TYPE == TYPE_命中
			TIMES VALUE, 1.30
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 1.30
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.30
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 0.8
	ELSEIF LOCAL < 80
		SIF TYPE == TYPE_命中
			TIMES VALUE, 1.40
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 1.40
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.40
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 0.7
	ELSEIF LOCAL < 99
		SIF TYPE == TYPE_命中
			TIMES VALUE, 1.45
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 1.45
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.45
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 0.6
	ELSEIF LOCAL >= 100
		SIF TYPE == TYPE_命中
			TIMES VALUE, 1.50
		SIF TYPE == TYPE_対空命中
			TIMES VALUE, 1.50
		SIF TYPE == TYPE_回避
			TIMES VALUE, 1.50
		SIF TYPE == TYPE_敵回避
			TIMES VALUE, 0.5
	ENDIF
RETURN VALUE




@SKILL_0_PERSONAL_13_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
