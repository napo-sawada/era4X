;パフォーマンスカードの定義
;-------------------------------------------------
;パフォーマンスカード名称
;-------------------------------------------------
@PERFORMANCE_CARDNAME_801
RESULTS:0 = 公開自慰

;-------------------------------------------------
;パフォーマンス分類
;-------------------------------------------------
@PERFORMANCE_CATEGORY_801
#DIMS カード属性
VARSET カード属性
カード属性 '= DIC_SET(カード属性, "属性", "GESTURE")
カード属性 '= DIC_SET(カード属性, "演目", "LEWD")
RESULTS = %カード属性%


;-------------------------------------------------
;パフォーマンスコマンド名称
;-------------------------------------------------
@PERFORMANCE_COMNAME_801
RESULTS:0 = 公開自慰

;-------------------------------------------------
;アンロック条件
;-------------------------------------------------
@PERFORMANCE_AVAILABLE_801
RETURN 1

;-------------------------------------------------
;セット可能なキャラクター条件
;-------------------------------------------------
@PERFORMANCE_SETTABLE_BY_CHARA_801(ARG:0)
IF ABL:(ARG:0):露出 >= 5 && CFLAG:(ARG:0):陥落済み
    RETURN 1
ENDIF
RETURN 0

;-------------------------------------------------
;キャラがカードをセット可能できない場合の説明
;-------------------------------------------------
@PERFORMANCE_SETTABLE_DESC_801(ARG:0)
RESULTS:0 = 陥落済かつ露出5以上が必要 

;-------------------------------------------------
;演目で使用可能となる条件
;-------------------------------------------------
@PERFORMANCE_PLAYABLE_801
RETURN 1

;-------------------------------------------------
;メイン処理
;-------------------------------------------------
@PERFORMANCE_MAIN_801(ARG:0)
#DIMS カード効果
VARSET カード効果
カード効果 '= DIC_SET(カード効果, "補正値変更", "0,0,0,15,")
カード効果 '= DIC_SET(カード効果, "トークン変更","快楽,1,")
RESULTS = %カード効果%

;-------------------------------------------------
;プレイ時のメッセージ
;-------------------------------------------------
@PERFORMANCE_PLAY_MESSAGE_801(ARG:0)
IF !HAS_PENIS(ARG:0)
    PRINTFORML %ANAME(ARG:0)%は聴衆に見せつけように脚を広げると、悩ましげな嬌声をあげながら自身の秘部を指で弄んだ
    CALLF FuncTagSetText_Print(@"聴衆の視線があらわになった%ANAME(ARG:0)%の@C:{カラー_LE}@%STR_BODY("クリ", (ARG:0))%@/C@に釘付けになる", 1)
    CALL FUCK(ARG:0, "Ｃ, 自慰, 欲望, 露出")
ELSE
    PRINTFORML %ANAME(ARG:0)%は聴衆に見せつけように脚を広げると、悩ましげな嬌声をあげながら自身の肉茎をしごき上げた
    CALL FUCK(ARG:0, "Ｃ, 自慰, 欲望, 露出, 射精")
ENDIF


;-------------------------------------------------
;ステータス描写
;-------------------------------------------------
@PERFORMANCE_STATUS_DESC_801
RESULTS = 補正:LE + 15\% 快楽トークン+1
