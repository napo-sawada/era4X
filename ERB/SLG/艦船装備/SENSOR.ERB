;-----------------------------------
; センサー
;-----------------------------------
@GET_SENSOR(ID)
#DIM ID

SELECTCASE ID
	CASE 1
		SENSOR = 1
		EQUIP_NAME = TIER1汎用センサー
		PRICE = 10000
	CASE 2
		SENSOR = 2
		EQUIP_NAME = TIER2汎用センサー
		PRICE = 20000
	CASE 3
		SENSOR = 3
		EQUIP_NAME = TIER3汎用センサー
		PRICE = 40000
	CASEELSE
		EQUIP_NAME = ---
ENDSELECT

;-----------------------------------
; センサーの説明
;-----------------------------------
@GET_SENSOR_DESCRIPTION(ID)
#DIM ID
CALL GET_SENSOR(ID)
SELECTCASE ID
	CASE 1 TO 3
		PRINTFORML %EQUIP_NAME%
		PRINTFORML センサー範囲: {SENSOR}
	CASEELSE
		PRINTFORML = ---
ENDSELECT

;-----------------------------------
; センサーの資材
;-----------------------------------
@GET_SENSOR_RESOURCE(USE_RESOURCE, ID)
#DIM REF USE_RESOURCE
#DIM ID

SELECTCASE ID
	CASE 1
		USE_RESOURCE:タイラーセンサー = 3
	CASE 2
		USE_RESOURCE:タイラーセンサー = 6
	CASE 3
		USE_RESOURCE:タイラーセンサー = 9
ENDSELECT

;-----------------------------------
; センサーの換装
;-----------------------------------
@CHANGE_SENSOR(SHIP_ID, SALE_COUNTRY, EQUIP_ID)
#DIM SHIP_ID
#DIM SALE_COUNTRY
#DIM EQUIP_ID
#DIM USE_RESOURCE, ITEM_LENGTH
#DIM ITER
#DIM RESOURCE_COUNT
#DIM LACK_RESOURCE, ITEM_LENGTH
#DIM CAN_BUY

CAN_BUY = 1

CALL GET_SENSOR_RESOURCE(USE_RESOURCE, EQUIP_ID)
FOR ITER, 0, ITEM_LENGTH
	SIF USE_RESOURCE:ITER == 0
		CONTINUE
	RESOURCE_COUNT = GET_RESOURCE_NUM(SHIP_POSITION:SHIP_ID, SALE_COUNTRY, ITER)
	IF USE_RESOURCE:ITER > RESOURCE_COUNT
		LACK_RESOURCE:ITER += USE_RESOURCE:ITER - RESOURCE_COUNT
		CAN_BUY = 0
	ENDIF
NEXT

IF CAN_BUY == 0
	IF SHIP_COUNTRY:SHIP_ID == PLAYER_COUNTRY
		SETCOLOR 0XFF0000
		PRINTL [CAUTION] 資源不足で装備を購入できませんでした
		RESETCOLOR
	ENDIF
	RETURN 0
ENDIF

FOR ITER, 0, ITEM_LENGTH
	SIF USE_RESOURCE:ITER == 0
		CONTINUE
	RESULT = REMOVE_RESOURCE(SHIP_COUNTRY:SHIP_ID, SALE_COUNTRY, ITER, USE_RESOURCE:ITER)
	SHIP_EQUIP:センサー:SHIP_ID = EQUIP_ID
NEXT
RETURN 1