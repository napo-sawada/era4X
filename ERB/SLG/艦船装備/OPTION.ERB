;-----------------------------------
; オプション
;-----------------------------------
@GET_OPTION(ID)
#DIM ID

SELECTCASE ID
	CASE 1
		EQUIP_NAME = 小型移乗コンテナ
		PRICE = 10000
		RETURN 1
	CASEELSE
		EQUIP_NAME = ---
ENDSELECT

;-----------------------------------
; オプションの説明
;-----------------------------------
@GET_OPTION_DESCRIPTION(ID)
#DIM ID
CALL GET_OPTION(ID)
SELECTCASE ID
	CASE 1
		PRINTFORML %EQUIP_NAME%
		PRINTFORML 一人乗りの移乗コンテナ。完全放棄された艦船を乗っ取るのに使う。
	CASEELSE
		PRINTFORML = ---
ENDSELECT

;-----------------------------------
; オプションの資材
;-----------------------------------
@GET_OPTION_RESOURCE(USE_RESOURCE, ID)
#DIM REF USE_RESOURCE
#DIM ID

SELECTCASE ID
	CASE 1
		USE_RESOURCE:装甲合金 = 1
		USE_RESOURCE:シールド発生装置 = 1
ENDSELECT

;-----------------------------------
; オプションの換装
;-----------------------------------
@CHANGE_OPTION(SHIP_ID, SALE_COUNTRY, EQUIP_ID)
#DIM SHIP_ID
#DIM SALE_COUNTRY
#DIM EQUIP_ID
#DIM USE_RESOURCE, ITEM_LENGTH
#DIM ITER
#DIM RESOURCE_COUNT
#DIM LACK_RESOURCE, ITEM_LENGTH
#DIM CAN_BUY

CAN_BUY = 1

CALL GET_OPTION_RESOURCE(USE_RESOURCE, EQUIP_ID)
FOR ITER, 0, ITEM_LENGTH
	SIF USE_RESOURCE:ITER == 0
		CONTINUE
	RESOURCE_COUNT = GET_RESOURCE_NUM(SHIP_POSITION:SHIP_ID, SALE_COUNTRY, ITER)
	IF USE_RESOURCE:ITER > RESOURCE_COUNT
		LACK_RESOURCE:ITER += USE_RESOURCE:ITER - RESOURCE_COUNT
		CAN_BUY = 0
	ENDIF
NEXT

IF CAN_BUY == 0
	IF SHIP_COUNTRY:SHIP_ID == PLAYER_COUNTRY
		SETCOLOR 0XFF0000
		PRINTL [CAUTION] 資源不足で装備を購入できませんでした
		RESETCOLOR
	ENDIF
	RETURN 0
ENDIF

FOR ITER, 0, ITEM_LENGTH
	SIF USE_RESOURCE:ITER == 0
		CONTINUE
	RESULT = REMOVE_RESOURCE(SHIP_COUNTRY:SHIP_ID, SALE_COUNTRY, ITER, USE_RESOURCE:ITER)
	SHIP_EQUIP:オプション:SHIP_ID = EQUIP_ID
NEXT
RETURN 1
